{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Narrow capabilities for CES-created desktop windows",
  "windows": ["overlay", "thumbnail", "viewer", "preferences"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
