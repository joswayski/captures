{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Narrow capabilities for Captures desktop windows",
  "windows": ["overlay", "thumbnail", "viewer", "viewer-*", "history", "preferences", "startup"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
