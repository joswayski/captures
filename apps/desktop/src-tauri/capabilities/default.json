{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Narrow capabilities for Captures desktop windows",
  "windows": ["overlay", "thumbnail", "viewer", "viewer-*", "preferences", "startup"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
