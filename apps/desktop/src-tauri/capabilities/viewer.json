{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "viewer",
  "description": "Allow full-size preview windows to complete their close lifecycle",
  "windows": ["viewer-*"],
  "permissions": [
    "core:window:allow-close",
    "core:window:allow-destroy"
  ]
}
