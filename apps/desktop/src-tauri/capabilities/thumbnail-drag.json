{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "thumbnail-drag",
  "description": "Allow the quick-access preview stack to start native file drags",
  "windows": ["thumbnail"],
  "permissions": [
    "drag:default"
  ]
}
