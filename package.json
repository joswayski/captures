{
  "name": "ces",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run tauri:dev --workspace @ces/desktop",
    "build": "npm run tauri:build --workspace @ces/desktop",
    "check": "npm run check --workspace @ces/desktop",
    "test": "npm run test --workspace @ces/desktop"
  }
}

