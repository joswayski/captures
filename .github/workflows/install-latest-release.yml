name: Install latest Preview

on:
  pull_request:
    paths:
      - ".github/workflows/install-latest-release.yml"
      - "scripts/install-latest-release.mjs"
      - "scripts/install-latest-release.test.mjs"
      - "scripts/preview-release.mjs"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  discover:
    name: Find a published Preview
    runs-on: ubuntu-24.04
    outputs:
      available: ${{ steps.release.outputs.available }}
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Check whether a Preview is published
        id: release
        shell: bash
        run: |
          count="$(
            gh api \
              --paginate \
              "repos/${GITHUB_REPOSITORY}/releases?per_page=100" \
              | jq -s '[.[][] | select(.draft == false and .prerelease == true and (.tag_name | test("^v[0-9]{4}\\.[0-9]{2}\\.[0-9]{2}\\.[1-9][0-9]?$")))] | length'
          )"
          if (( count > 0 )); then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "No Preview exists yet. Installer verification will begin after the first Preview is published." >> "$GITHUB_STEP_SUMMARY"
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

  install:
    name: Install latest Preview (${{ matrix.name }})
    needs: discover
    if: needs.discover.outputs.available == 'true'
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: macOS Apple Silicon
            os: macos-26
          - name: Windows x64
            os: windows-2025
          - name: Ubuntu x64
            os: ubuntu-24.04
    runs-on: ${{ matrix.os }}
    timeout-minutes: 75
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install and verify the newest complete Preview
        run: node scripts/install-latest-release.mjs --no-launch
